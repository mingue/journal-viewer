[package]
build = "build.rs"
edition = "2021"
links = "systemd"
name = "journal-viewer"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bitflags = "1.3"
libc = "0.2"
